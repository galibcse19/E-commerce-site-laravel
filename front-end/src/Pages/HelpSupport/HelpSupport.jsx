import React from 'react';
import SharedTitle from '../../Shared/SharedTitle';

const HelpSupport = () => {
    return (
        <div className="min-h-screen bg-gray-100 p-6">
            {/* Top Section - Header */}
            <SharedTitle heading={'Help & Support'} subHeading={'Find answers to your questions and get the support you need'}></SharedTitle>
        </div>
    );
};

export default HelpSupport;
